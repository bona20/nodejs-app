pipeline{
    agent any

    stages{
        stage ('Dep install'){
            agent { docker { image 'nodejs'}}
            steps{
                sh 'npm cash clean'
               //  sh 'sudo chown -R 995:993 "/.npm"'
                sh 'npm install'
            }
        }
    }
}